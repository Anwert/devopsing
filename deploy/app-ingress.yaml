apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aspnethelloworld-ingress
spec:
  rules:
    - http:
        paths:
          - path: /app
            pathType: Prefix
            backend:
              service:
                name: asphelloworld-service
                port:
                  number: 80