apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: aspnethelloworld-ingress
spec:
  rules:
    - http:
        paths:
          - backend:
              serviceName: asphelloworld-service
              servicePort: 80
            path: /